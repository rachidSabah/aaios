"""AAiOS Kernel (L1).

The irreducible core: Event Bus, State Manager, Configuration Manager,
Logging System, Telemetry collector, DI container, Tool Registry,
Prompt Registry, and the Gateway (the only package allowed to perform I/O).

The kernel has zero knowledge of agents, models, or user interfaces.
"""

__all__: list[str] = []
